{
  "name": "db",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "seed": "tsx seed.ts",
    "reset": "prisma migrate reset --schema schema.prisma --force && npm run seed",
    "install": "echo '✅ DB workspace: Prisma schema and seed files ready.'",
    "build": "echo 'No build step for db'",
    "generate": "prisma generate",
    "postinstall": "npm run generate",
    "sync": "npm run migrate && npm run generate",
    "migrate": "prisma migrate deploy --schema=schema.prisma",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.11.1"
  },
  "devDependencies": {
    "prisma": "^6.11.1",
    "tsx": "^4.7.0"
  }
}
