{
  "name": "db",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "seed": "tsx seed.ts",
    "reset": "prisma migrate reset --schema schema.prisma --force && npm run seed",
    "install": "echo '✅ DB workspace: Prisma schema and seed files ready.'",
    "build": "echo 'No build step for db'",
    "generate": "prisma generate",
    "postinstall": "npm run generate",
    "sync": "prisma migrate dev --schema=schema.prisma && npm run generate",
    "deploy": "prisma migrate deploy --schema=schema.prisma && npm run generate",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "prisma": "^7.8.0",
    "tsx": "^4.7.0"
  }
}
